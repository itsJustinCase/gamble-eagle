name: Romania Scraper
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
- name: Install Dependencies
        run: |
          pip install -r requirements.txt
          # Ensure the package manager is updated before installing system deps
          sudo apt-get update
          playwright install chromium
          # Use --with-deps to let Playwright handle the specific library names
          playwright install-deps chromium
          
      - name: Execute Scraper
        run: python fetch_romania.py
        
      - name: Push Results
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add romania.csv
          git commit -m "Update domains [skip ci]" || exit 0
          git push
